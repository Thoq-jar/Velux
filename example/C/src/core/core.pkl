amends "../../../../pkl/BuildConfig.pkl"

builds = Map(
  "core", new Build {
    name = "core"
    sources = Set("*.c")
    language = new C {
      buildType = "shared"
      libraries = List()
      includePaths = List(".")
      extraFlags = List("-fPIC", "-shared")
    }
    output = "core"
  }
)
