amends "../../../pkl/BuildConfig.pkl"

builds = Map(
  "app", new Build {
    name = "app"
    sources = Set("*.c")
    language = new C {}
    buildType = "release"
    dependencies = List("core")
    includePaths = List("../core")
    extraFlags = List()
    output = "velux-app"
  }
)
