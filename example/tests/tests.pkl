amends "../../pkl/BuildConfig.pkl"

builds = Map(
  "tests", new Build {
    name = "tests"
    sources = Set("*.c")
    language = new C {}
    buildType = "debug"
    dependencies = List("core")
    includePaths = List("../src/core")
    extraFlags = List()
    output = "test_runner"
  }
)
